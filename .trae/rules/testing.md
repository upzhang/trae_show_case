# 测试规范

- 单元测试文件名：`*.test.ts` / `*.test.tsx`，与源码文件同目录
- API 测试文件位于项目根 `tests/api` 目录
- E2E 测试位于项目根 `tests/e2e` 目录
- API 测试必须在 `beforeAll` 中启动 `app` 并使用 Supertest
- 每个 API 至少包含一条"权限不足"用例与一条"成功"用例
- 测试中避免依赖真实网络、真实数据库（当前仓库使用内存存储，可直接使用）

## 可接受的跳过

- 若测试依赖真实浏览器且在 CI 不稳定，可标记 `test.skip` 并在 CI 配置中说明
