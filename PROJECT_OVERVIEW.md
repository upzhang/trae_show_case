# Trae Enterprise Validation Demo

一个面向企业级 SaaS 平台的全栈演示项目，覆盖多租户管理、RBAC 权限、审批流、发布管理、工单系统等核心业务场景。

## 代码规模

| 模块 | 行数 |
|---|---|
| apps/api（路由 + 服务 + 中间件 + 工具库） | 8,501 |
| apps/web-admin（17 个页面 + 组件） | 3,634 |
| packages/shared（类型定义） | 813 |
| tests（11 个测试文件） | 3,612 |
| **合计** | **16,757** |

## 技术栈

| 层 | 技术 |
|---|---|
| 前端 | React + TypeScript + Vite |
| 后端 | Express + TypeScript |
| 共享类型 | Monorepo（pnpm workspace） |
| 测试 | Vitest + Supertest（213 个用例全部通过） |
| 数据 | 内存仓库模式，内置 20 个模拟租户 |

## 功能模块

### 核心业务（8 个模块）

| 模块 | 说明 |
|---|---|
| **多租户管理** | 20 个模拟租户，覆盖制造、金融、医疗、航天等行业 |
| **用户与角色** | 平台管理员 / 租户管理员 / 审计员 / 发布经理 / 普通成员 5 种角色 |
| **RBAC 权限** | 22 个细粒度权限码，中间件级鉴权 |
| **审批流** | 审批请求的创建、流转、批准/拒绝 |
| **发布管理** | 发布记录的创建、部署、回滚 |
| **审计日志** | 全量操作审计，支持按租户/用户/操作类型检索 |
| **风险监控** | 支持风险登记、状态跟踪 |
| **活动事件** | 平台活动事件记录与查询 |

### 扩展业务（9 个模块）

| 模块 | 说明 |
|---|---|
| **Webhook** | 事件订阅、投递历史、重试机制 |
| **API Token** | Token 生命周期管理、权限校验、过期处理 |
| **团队管理** | 团队 CRUD、成员管理、角色分配 |
| **系统集成** | Slack / Jira / GitHub / Stripe 等多平台集成 |
| **功能开关** | 特性开关、租户级覆盖、启用/禁用/灰度 |
| **工单系统** | 工单创建、分配、状态流转、评论、统计 |
| **订阅管理** | 免费/Pro/标准/企业版，升降级、续费、取消 |
| **发票管理** | 发票生成、退款、PDF 导出、统计分析 |
| **通知中心** | 多渠道通知、批量推送、定时发送、已读管理 |

## 项目结构

```
trae-enterprise-validation-demo/
├── apps/
│   ├── api/          # Express API 服务（18 个路由模块 + 16 个服务层）
│   └── web-admin/    # React 管理后台（17 个页面）
├── packages/
│   └── shared/       # 共享类型定义
└── tests/
    └── api/          # 11 个测试文件，213 个集成测试用例
```

## 快速开始

```bash
pnpm install
pnpm dev:api     # 启动 API 服务 → http://localhost:3001
pnpm dev:web     # 启动管理后台 → http://localhost:5173
pnpm test        # 运行全部测试
pnpm typecheck   # 类型检查
```

## 演示要点

- **零依赖部署**：无需数据库，内存仓库模式即开即用
- **完整 RBAC**：从类型定义 → 中间件 → 路由鉴权的全链路权限控制
- **真实业务场景**：20 个租户、100+ 用户、覆盖 SaaS 平台典型业务流程
- **全量测试覆盖**：213 个集成测试，覆盖所有 API 模块的正常/异常路径
